// const express = require("express");
// const GroupRouter = express.Router();
// const { getGroup, editGroup, createGroup } = require("../controller/group.controller");

// GroupRouter.route("/group").post(createGroup).
// GroupRouter.route("/group/:id").get(getGroup).patch(editGroup).delete(deleteGroup);

// module.exports = GroupRouter;
